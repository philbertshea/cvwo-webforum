<%= form_with(model: fpost) do |form| %>
  <% if fpost.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(fpost.errors.count, "error") %> prohibited this fpost from being saved:</h2>

      <ul>
        <% fpost.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= react_component("MyForm", {fpost: @fpost}) %>
<% end %>
